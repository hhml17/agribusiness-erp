# AZURE APP SERVICE - Backend Configuration Template
# Copiar estos valores en Azure Portal → App Service → Configuration → Application settings

# ===========================================
# BASE DE DATOS
# ===========================================
DATABASE_URL=sqlserver://agribusiness.database.windows.net:1433;database=agribusiness;user=agribusiness@agribusiness;password=TU_PASSWORD_AQUI;encrypt=true;trustServerCertificate=false;hostNameInCertificate=*.database.windows.net;loginTimeout=30

# ===========================================
# AZURE AD / MICROSOFT ENTRA ID
# ===========================================
# Frontend App Registration ID
AZURE_CLIENT_ID=185a1a46-e8fe-4dc9-97b0-22629f47f8be

# Backend App Registration ID
AZURE_BACKEND_CLIENT_ID=67a98f45-8f1e-4212-9f0f-112dcd12d628

# Tenant ID (obtener de Entra ID → Overview)
AZURE_TENANT_ID=TU_TENANT_ID_AQUI

# Client Secret (obtener de Entra ID → App Registration → Certificates & secrets)
AZURE_CLIENT_SECRET=TU_CLIENT_SECRET_AQUI

# ===========================================
# API CONFIGURATION
# ===========================================
NODE_ENV=production
PORT=8080

# CORS Origins (separados por comas, SIN ESPACIOS)
ALLOWED_ORIGINS=https://erp.agribusiness.com.py,https://thankful-ground-083e4cb10.3.azurestaticapps.net

# JWT Secret (genera uno nuevo y seguro, mínimo 32 caracteres)
# Puedes generar uno con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=GENERA_UN_SECRET_SEGURO_AQUI_MINIMO_32_CARACTERES

# ===========================================
# LOGGING
# ===========================================
LOG_LEVEL=info

# ===========================================
# FRONTEND ENVIRONMENT VARIABLES
# Estos van en GitHub → Settings → Secrets and variables → Actions
# ===========================================

# GitHub Secrets (valores sensibles):
# - VITE_AZURE_CLIENT_ID: 185a1a46-e8fe-4dc9-97b0-22629f47f8be
# - VITE_AZURE_TENANT_ID: TU_TENANT_ID_AQUI
# - AZURE_STATIC_WEB_APPS_API_TOKEN_THANKFUL_GROUND_083E4CB10: (ya configurado)
# - AZURE_WEBAPP_PUBLISH_PROFILE: (descargar de Azure Portal)

# GitHub Variables (valores públicos):
# - VITE_API_URL: https://agribusiness.azurewebsites.net/api
# - VITE_REDIRECT_URI: https://erp.agribusiness.com.py
# - VITE_DEV_MODE: false
